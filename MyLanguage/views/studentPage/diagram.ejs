<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js@2.9.4/dist/Chart.min.js"></script>
</head>
<body>
    <div>
        <canvas id="chart" width="100%" height="30vh"></canvas>
        <br><br>
    </div>
    <script>
        let xlabel = []
        // await getData()
        let ctx = document.getElementById('chart').getContext('2d');
        let myChart = new Chart(ctx, {
            type: 'bar',
            data: {
                labels: xlabel,
                datasets: [{
                    label: 'Kelas yang paling banyak diikuti',
                    data: [12, 19, 3, 5, 2, 3],
                    backgroundColor: [
                        'rgba(255, 99, 132, 0.2)',                      
                    ],
                    borderColor: [
                        'rgba(255, 99, 132, 1)',
                    ],
                    borderWidth: 1
                }]
            }
        });
        data.forEach(element => { 
             xlabel.push(element.class_name) 
         }); 
        
        </script>
</body>
</html>